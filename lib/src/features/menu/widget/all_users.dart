import 'package:flutter/material.dart';

class AllUsersTile extends StatelessWidget {
  const AllUsersTile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}